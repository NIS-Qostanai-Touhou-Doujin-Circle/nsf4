# Database configuration for local development
# Make sure you have a MySQL/MariaDB instance running locally
# or use the Docker database from docker-compose
DATABASE_URL=mysql://root:root@localhost:3306/nsf

# If you want to connect to the Docker database, use:
# DATABASE_URL=mysql://nsf_user:nsf_password@127.0.0.1:3306/nsf

# Other environment variables
PORT=5123
MEDIA_SERVER_URL=rtmp://localhost:1935
SCREENSHOT_INTERVAL_SECONDS=10
SCREENSHOT_QUALITY=80
RUST_LOG=info
